services:
  gateway:
    build:
      context: .
      dockerfile: Dockerfile
      target: gateway
      args:
        SERVICE: gateway
    environment:
      - KEYCLOAK_BASE_URL=http://keycloak:8080

  game-service:
    build:
      context: .
      dockerfile: Dockerfile
      target: game
      args:
        SERVICE: game

  identity-service:
    build:
      context: .
      dockerfile: Dockerfile
      target: identity
      args:
        SERVICE: identity
    environment:
      - KEYCLOAK_BASE_URL=http://keycloak:8080

  statistics-service:
    build:
      context: .
      dockerfile: Dockerfile
      target: statistics
      args:
        SERVICE: statistics
