http:
  host: 0.0.0.0
  port: 8080
  mode: debug
  shutdown_timeout: 10s
  cors:
    allowed_origins: [ "*" ]
    allowed_methods: [ "GET", "POST", "PUT", "DELETE", "PATCH", "OPTIONS" ]
    allowed_headers: [ "Origin", "Content-Type", "Accept", "Authorization" ]
    expose_headers: [ ]
    allow_credentials: true
    max_age: 24h

metrics:
  host: 0.0.0.0
  port: 9090

iam_service:
  host: localhost
  port: 50052

game_service:
  host: localhost
  port: 50051

game_websocket:
  host: localhost
  port: 8081

statistics_service:
  host: localhost
  port: 50053

keycloak:
  base_url:
  realm:
  public_client_id:
  public_client_secret:
  admin_client_id:
  admin_client_secret:

guest_secret: "change-me-in-production"
